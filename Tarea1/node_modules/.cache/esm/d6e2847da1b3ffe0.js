_a1b‍.x([["getToken",()=>getToken]]);const fetch = require('node-fetch');
const APIURL = 'https://api.softwareavanzado.world/index.php?option=token&api=oauth2';


       const getToken =()=>{
    let data={
        grant_type    : 'client_credentials',
        client_id     : 'haldamir.95@gmail.com',
        client_secret : 201314733
    };

    return new Promise(async(solve,reject)=>{
    fetch(APIURL, { 
        method: 'POST',
        headers:{
          'Content-Type': 'application/json',
          'Accept':'application/json'
        },
        body: JSON.stringify(data)
      
    })
    .then(res => {
        if(!res.ok){
            reject(res);
        }else{
            solve(res.json());
        }
    })
    .catch(err=>{
        reject(err);
    })
    })
};  
