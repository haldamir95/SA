let express;_a9d‍.x([["Server",()=>Server]]);_a9d‍.w("express",[["*",null,function(v){express=v}]]);

       class Server {
  constructor (port) {
    this.port = port
    this.app = express.default()
    this.app.use(express.json())
  }

  routers (route) {
    this.app.use(route)
  }

  listen () {
    this.app.listen(this.port, () => {
    })
  }
}
